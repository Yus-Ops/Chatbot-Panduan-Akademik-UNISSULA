# Salin file ini jadi .env.local lalu isi URL backend.
# Lokal (backend FastAPI di komputer sendiri):
NEXT_PUBLIC_API_URL=http://localhost:8000

# Produksi (Vercel): isi dengan URL publik dari Cloudflare Tunnel ke backend GPU lokal, mis:
# NEXT_PUBLIC_API_URL=https://nama-acak.trycloudflare.com
